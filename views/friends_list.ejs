<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Friends_list</title>
</head>
<body>
    <h1> Friends List </h1> 


    <!-- friends <- express로부터 전달받은 객체  -->
    <ul>
        <% for (let i=0; i < friends.length; i++) { %> 
            <a href='/web/friends/show/<%= friends[i]._id %>'>
                <li> <%= friends[i].name %>: <%= friends[i].species %>, <%= friends[i].age %> </li>
            </a>
        <% } %> 
    </ul>

</body>
</html>